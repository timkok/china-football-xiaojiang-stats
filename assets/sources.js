const BASE=document.body.dataset.root||'';fetch(`${BASE}data/sources.json`).then(r=>r.json()).then(sources=>{document.querySelector('#sourceRows').innerHTML=sources.map(s=>`<tr><td>${s.title}</td><td>${s.publisher}</td><td>${s.published_date||'<span class="muted">待补</span>'}</td><td>${s.source_type}</td><td><span class="badge">${s.source_priority}</span></td><td>${s.involved_players.length?s.involved_players.join('、'):'<span class="muted">尚未关联球员</span>'}</td><td>${s.involved_levels.join(' / ')||'<span class="muted">未关联</span>'}</td><td>${s.access_status}</td><td><a href="${s.url}" target="_blank" rel="noopener">打开</a></td><td>${s.archived_url?`<a href="${s.archived_url}">归档</a>`:'<span class="muted">无</span>'}</td></tr>`).join('')});
